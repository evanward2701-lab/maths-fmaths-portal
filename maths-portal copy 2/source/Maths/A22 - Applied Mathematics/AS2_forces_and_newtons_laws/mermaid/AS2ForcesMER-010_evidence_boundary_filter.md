# AS2ForcesMER-010

**Asset ID:** AS2ForcesMER-010  
**Source:** CCEA specification map + project evidence checklist  
**Related lesson section:** Syllabus Gap Check and Off-Spec Log  
**Purpose:** Show how cross-board Year 2 evidence is filtered through the CCEA AS2-FORCES boundary.

```mermaid
flowchart TD
    A["Evidence item"] --> B{"Matches CCEA AS2-FORCES?"}
    B -->|Yes| C["Use as core or supporting lesson content"]
    B -->|Partly| D["Use only matching parts"]
    B -->|No| E["Exclude from core lesson"]
    D --> F["Log boundary-risk note"]
    E --> G["Log off-spec exclusion"]
    C --> H["Map to LO ID"]
    F --> H
    H --> I["Include in lesson or asset plan"]
    G --> J["Do not assess as required content"]
```
